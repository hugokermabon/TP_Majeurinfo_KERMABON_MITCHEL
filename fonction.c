#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <sys/wait.h>
#include <time.h>
#include "fonction.h"

char Convert_Complex(char buffer){
    char buff_complex[BUFFSIZE];
    for (int i=strlen(buffer)){
        if (buffer[i]==['']){
            i++;
            int k =i+1;
            while (int j<i && k<strlen(buffer)){
                        
                    }
                    

                }
            }
}

